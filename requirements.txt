# FUKU Coffee uses ONLY Python stdlib — no pip dependencies needed.
# This file exists so deployment platforms (Render / Railway / etc.)
# detect the project as Python and skip the install step cleanly.
